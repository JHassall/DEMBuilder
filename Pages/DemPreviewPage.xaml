<UserControl x:Class="DEMBuilder.Pages.DemPreviewPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="Step 6: Preview DEM" FontSize="20" FontWeight="Bold" Margin="10"/>

        <Border Grid.Row="1" BorderBrush="Gray" BorderThickness="1" Margin="10">
            <Image x:Name="PreviewImage" Stretch="Uniform" />
        </Border>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="10">
            <Button Content="&lt; Back" x:Name="BackButton" Click="BackButton_Click" Margin="5" Padding="10,5"/>
            <Button Content="Next >" x:Name="NextButton" Click="NextButton_Click" Margin="5" Padding="10,5"/>
        </StackPanel>
    </Grid>
</UserControl>
